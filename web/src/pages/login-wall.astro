---
import { secureAccessWallResponse } from '../lib/secure-access-wall';

// Fallback if something requests /login-wall directly. Locked `/` returns
// the same Response from index.astro — do not rewrite here (custom-domain hang).
return secureAccessWallResponse(Astro.url.searchParams.get('denied') === '1');
---
